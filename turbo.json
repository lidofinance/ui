{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"],
      "inputs": ["src/**", "packages/**", "styles/**", "rollup.config.mjs", "tsconfig.json", "tsconfig.production.json", ".babelrc.json"]
    },
    "build-storybook": {
      "outputs": ["storybook-static/**"],
      "inputs": ["src/**", "packages/**", ".storybook/**"]
    },
    "test": {
      "dependsOn": ["build"]
    },
    "lint": {},
    "lint:css": {},
    "types": {}
  }
}
